{
  "name": "tradeloom-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "End-to-end tests driving the real API and the real client.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2"
  }
}
